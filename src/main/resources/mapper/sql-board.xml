<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.boot.mybatis.mapper.BoardMapper">

	<select id="selectBoardList" resultType="com.boot.mybatis.dto.BoardDto">
		<![CDATA[
			SELECT
				board_idx,
				title,
				hit_cnt,
				created_datetime
			FROM
				t_board
			WHERE
				deleted_yn = 'N'
			ORDER BY board_idx desc		
		]]>
	</select>
	
	<insert id="insertBoard" parameterType="com.boot.mybatis.dto.BoardDto">
	
	insert into t_board(title, contents, created_datetime, created_id )
	value
	(#{title},#{contents}, now(), 'admin')

	</insert>

</mapper>